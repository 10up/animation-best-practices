{
  "name": "animation-best-practices",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "10up-toolkit build --wp=false --format=none",
    "dev": "npm run build -- --watch --dev-server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "10up-toolkit": "^5.0.0",
    "html-loader": "^4.2.0",
    "html-webpack-plugin": "^5.5.0"
  },
  "10up-toolkit": {
    "libraryName": "TestExample",
    "entry": {
      "index": "./src/index.html"
    }
  },
  "browserslist": {
    "production": [
      ">0.5%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "license": "MIT"
}
